{
  "private": true,
  "scripts": {
    "test": "nbb --classpath src:test -e \"(require '[clojure.test :as t] 'aadhaar.main-test) (let [r (t/run-tests 'aadhaar.main-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
