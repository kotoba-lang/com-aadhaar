;; Row 18 over the canonical typed map, since the g18a refusal named the receiver.
(ns g18c (:export [probe]))
(defn m [] [:map :keyword :i64] (typed-map-new [:map :keyword :i64] :a 1 :b 2))
(defn probe [] :i64 (:a (m)))
