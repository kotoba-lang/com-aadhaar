;; Row 1: "string literals anywhere (in a def or a fn body)".
;; Both sites are CONSUMED by the exported function's return value.
(ns g01 (:export [probe]))
(def label "hello")
(defn probe [] :i64 (+ (string-byte-length label) (string-byte-length "world")))
