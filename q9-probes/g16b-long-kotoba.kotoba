;; Row 16, in Kotoba's vocabulary. Widening to a machine integer is `i64` for an
;; integer and f64-to-i64-truncating / f64-to-i64-checked for a float.
(ns g16b (:export [probe]))
(defn probe [] :i64 (+ (i64 3) (f64-to-i64-truncating (i64-to-f64-rounded 4))))
