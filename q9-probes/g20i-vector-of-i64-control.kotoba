;; Discriminating control for g20b/g20h: identical shape, i64 elements.
;; If this passes, the refusal is the map ELEMENT and not the vector constant.
(ns g20i (:export [probe]))
(def specs [1 2])
(defn probe [] :i64 (nth specs 0))
