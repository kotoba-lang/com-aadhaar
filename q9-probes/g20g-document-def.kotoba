;; Row 20 as a top-level constant on the document carrier, CONSUMED.
(ns g20g (:export [probe]))
(def specs (document [{:entity "ReserveData" :plural "reservedatas"}
                      {:entity "EModeCategory" :plural "emodecategories"}]))
(defn probe [] :i64 (document-count specs))
