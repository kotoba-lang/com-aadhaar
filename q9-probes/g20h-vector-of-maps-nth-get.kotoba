;; Row 20, reading the constant without `count`, to see whether g20b's
;; "expected i64, got map" came from the projection or from the constant.
(ns g20h (:export [probe]))
(def specs [{:a 1} {:a 2}])
(defn probe [] :i64 (get (nth specs 0) :a))
