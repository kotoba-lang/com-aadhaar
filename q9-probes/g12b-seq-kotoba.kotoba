;; Row 12, in Kotoba's vocabulary. `seq` is Clojure's nil-punning emptiness
;; test plus a seq view; the pair-chain heads spell both halves.
(ns g12b (:export [probe]))
(def xs [1 2 3])
(defn probe [] :i64 (if (empty? xs) 0 (+ (first xs) (count (rest xs)))))
