;; Row 20: a top-level vector-of-maps constant that is USED. The previous round
;; recorded this PASS with a def nothing consumed; here the export's return
;; value reaches it.
(ns g20 (:export [probe]))
(def specs [{:entity "ReserveData" :plural "reservedatas"}
            {:entity "EModeCategory" :plural "emodecategories"}])
(defn probe [] :i64 (count specs))
