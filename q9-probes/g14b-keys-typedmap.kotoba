;; Row 14, over the canonical typed map [:map K V].
(ns g14b (:export [probe]))
(defn m [] [:map :keyword :i64] (typed-map-new [:map :keyword :i64] :a 1 :b 2))
(defn probe [] :i64 (count (keys (m))))
