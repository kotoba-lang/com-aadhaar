;; Control for the rule the whole round rests on: is an UNUSED definition
;; type-checked? The def here is the vector-of-maps constant that g20j refuses
;; when the export reaches it. Nothing reaches it here.
(ns g00a (:export [probe]))
(def specs [{:a 1} {:a 2}])
(defn probe [] :i64 1)
