;; Row 10, in Kotoba's vocabulary: filter with the complement predicate.
(ns g10b (:export [probe]))
(def xs [1 2 3])
(defn small? [a :i64] :bool (not (> a 1)))
(defn probe [] :i64 (count (filter small? xs)))
